{
  "private": true,
  "workspaces": [
    "packages/client",
    "packages/server"
  ],
  "scripts": {
    "start:server": "yarn workspace server nodemon --exec babel-node src/index.js",
    "start:web": "yarn workspace client yarn start"
  }
}
